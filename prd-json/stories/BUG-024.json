{
  "id": "BUG-024",
  "title": "Ink UI blocks Ralph - doesn't auto-exit after rendering",
  "description": "The React Ink UI blocks Ralph from continuing because it waits for keyboard input that never works. Ralph calls the Ink UI, it renders, then waits forever for 'q' press.\n\nTwo issues:\n1. Keyboard input (useInput) doesn't work - can't press 'q' to quit\n2. Ink UI should auto-exit after rendering, not block waiting for input\n\nFix approach:\n- For 'startup' and 'iteration' modes: render once then exit immediately\n- For 'live' mode: keep running with file watch updates\n- Remove keyboard handler for non-live modes (not needed)",
  "type": "bug",
  "priority": "high",
  "storyPoints": 3,
  "status": "pending",
  "acceptanceCriteria": [
    {"text": "=== AUTO-EXIT FIX ===", "checked": true},
    {"text": "Add auto-exit after render for 'startup' mode", "checked": true},
    {"text": "Add auto-exit after render for 'iteration' mode", "checked": true},
    {"text": "Keep 'live' mode running with file watching", "checked": true},
    {"text": "Remove 'Press q to quit' message for non-live modes", "checked": true},
    {"text": "=== KEYBOARD FIX (live mode only) ===", "checked": true},
    {"text": "Debug why useInput hook doesn't receive events", "checked": true},
    {"text": "Check if raw mode is properly enabled for stdin", "checked": true},
    {"text": "Test: Press 'q' exits live mode cleanly", "checked": true},
    {"text": "=== TESTING ===", "checked": true},
    {"text": "Test: ralph 1 --ui-ink completes without hanging", "checked": true},
    {"text": "Test: Ink UI renders startup then exits", "checked": true},
    {"text": "Test: Ink UI renders iteration then exits", "checked": true},
    {"text": "Run CodeRabbit review - must pass (or create BUG if unfixable)", "checked": true},
    {"text": "Commit: fix: BUG-024 Ink UI auto-exit for non-live modes", "checked": false}
  ],
  "dependencies": [],
  "passes": false
}
